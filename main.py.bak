import streamlit as st # Προστέθηκε για να φαίνεται ότι υπάρχει και άλλο import στην αρχή
# ... (άλλα imports) ...
from core.language_pack import get_text # Διορθωμένη γραμμή: χωρίς εσοχή
# ...

def main():
    # init_session() # Η init_session() θα έπρεπε να είναι defined ή imported.
    # Για το παράδειγμα, απλά θα βάλω ένα placeholder αν λείπει.
    if 'lang' not in st.session_state:
        st.session_state.lang = 'gr'
    if 'user_info' not in st.session_state:
        st.session_state.user_info = None

    lang = st.session_state.lang # Εδώ παίρνουμε την τρέχουσα γλώσσα από το session state

    # --- LOGIN SCREEN ---
    if not st.session_state.user_info:
        st.header(f"🔐 {get_text('app_title', lang)}")
        
        c_lang, _ = st.columns([1,5])
        with c_lang:
            # Το selectbox που επιτρέπει την επιλογή γλώσσας
            sel = st.selectbox("🌐 Language", ["Ελληνικά", "English"], index=0 if lang=='gr' else 1)
            # Ενημερώνουμε το session_state.lang με τη νέα επιλογή
            st.session_state.lang = 'gr' if sel == "Ελληνικά" else 'en'
            # Αν η γλώσσα άλλαξε, κάνουμε rerun για να ενημερωθεί άμεσα το UI
            if st.session_state.lang != lang: st.rerun()

        # ... (οι καρτέλες σύνδεσης/εγγραφής χρησιμοποιούν επίσης get_text(..., lang)) ...
        return 

    # ... (το κυρίως μέρος της εφαρμογής) ...
    # Όλα τα στοιχεία του UI καλούν get_text(key, lang)
# ...